# Руководство по интеграции CMS для Газобетон Online

**Дата создания:** 13 ноября 2025  
**Цель:** Добавить удобную админку для управления контентом, сохранив возможность работы через код и оптимизацию Next.js

---

## 🎯 Требования

1. ✅ Сохранить возможность изменять/создавать новое через код (с AI помощником)
2. ✅ Сохранить оптимизацию Next.js (SSG, быстрая загрузка)
3. ✅ Удобная админка для редактирования контента
4. ✅ Данные в репозитории (Git-based) или синхронизация с кодом

---

## 🏆 Рекомендуемые решения

### Вариант 1: Tina CMS (Git-based) ⭐ **РЕКОМЕНДУЕТСЯ**

**Почему подходит:**
- ✅ Данные хранятся в файлах репозитория (markdown, JSON, TypeScript)
- ✅ Можно продолжать работать через код
- ✅ Админка работает локально или через GitHub
- ✅ Не влияет на оптимизацию (SSG остается)
- ✅ Бесплатный для открытых проектов

**Как работает:**
- Контент редактируется в админке Tina
- Изменения сохраняются в файлы (markdown/JSON/TS)
- Git коммитит изменения
- Next.js пересобирает страницы

**Что можно вынести в CMS:**
- Статьи (`construction-articles/*.md`)
- Региональный контент (`data/regional-content.ts`)
- FAQ (`lib/faq.ts`)
- Мета-теги и SEO-тексты

**Установка:**
```bash
npm install tinacms @tinacms/cli
npx @tinacms/cli init
```

**Пример интеграции:**
```tsx
// app/construction/[slug]/page.tsx
import { TinaMarkdown } from "tinacms/dist/rich-text";
import { client } from "../../../../tina/__generated__/client";

export default async function ArticlePage({ params }) {
  const { data } = await client.queries.article({
    relativePath: `${params.slug}.md`,
  });
  
  return <TinaMarkdown content={data.article.body} />;
}
```

**Плюсы:**
- Полный контроль через Git
- Работа через код + админка
- Бесплатно
- Не нужен отдельный сервер

**Минусы:**
- Требует настройки схемы контента
- Админка работает только в dev режиме (или через Tina Cloud)

---

### Вариант 2: Payload CMS (Headless, локальный)

**Почему подходит:**
- ✅ Можно настроить локальное хранилище (файлы или SQLite)
- ✅ Мощная админка
- ✅ TypeScript из коробки
- ✅ Можно синхронизировать с кодом

**Как работает:**
- Payload работает как отдельный сервис
- Данные в базе (MongoDB/PostgreSQL) или локальных файлах
- Next.js получает данные через API или напрямую из БД

**Установка:**
```bash
npx create-payload-app@latest
```

**Плюсы:**
- Профессиональная админка
- Гибкая настройка полей
- Можно использовать локально

**Минусы:**
- Требует базу данных (или настройку файлового хранилища)
- Нужен отдельный процесс для CMS
- Может усложнить деплой

---

### Вариант 3: Sanity (Headless, облачный)

**Почему подходит:**
- ✅ Облачная админка (не нужен свой сервер)
- ✅ Бесплатный план для небольших проектов
- ✅ Можно синхронизировать с кодом через API

**Как работает:**
- Контент редактируется в Sanity Studio (облако)
- Next.js получает данные через API
- Можно настроить ISR (Incremental Static Regeneration)

**Установка:**
```bash
npm install @sanity/client @sanity/image-url next-sanity
npx sanity init
```

**Плюсы:**
- Не нужен свой сервер
- Отличная админка
- Бесплатный план

**Минусы:**
- Данные в облаке (не в репозитории)
- Зависимость от внешнего сервиса
- Нужна синхронизация с кодом

---

### Вариант 4: Strapi (Headless, self-hosted)

**Почему подходит:**
- ✅ Можно развернуть локально
- ✅ Мощная админка
- ✅ Open Source

**Как работает:**
- Strapi работает как отдельный сервис
- Данные в базе (SQLite/PostgreSQL)
- Next.js получает данные через REST API или GraphQL

**Установка:**
```bash
npx create-strapi-app@latest my-cms
```

**Плюсы:**
- Полный контроль
- Гибкая настройка
- Можно использовать локально

**Минусы:**
- Требует базу данных
- Нужен отдельный процесс
- Может усложнить деплой

---

## 💡 Рекомендация: Tina CMS

Для вашего проекта **лучше всего подойдет Tina CMS**, потому что:

1. **Данные остаются в репозитории** — можно продолжать работать через код
2. **Не влияет на оптимизацию** — SSG остается, страницы генерируются статически
3. **Удобная админка** — для редактирования контента без кода
4. **Git-based** — все изменения видны в Git, можно откатывать
5. **Бесплатно** — для открытых проектов

---

## 📋 План интеграции Tina CMS

### Шаг 1: Установка

```bash
npm install tinacms @tinacms/cli
npx @tinacms/cli init
```

### Шаг 2: Настройка схемы контента

Создать схемы для:
- Статей (`construction-articles/*.md`)
- Регионального контента (`data/regional-content.ts`)
- FAQ (`lib/faq.ts`)

### Шаг 3: Миграция контента

- Конвертировать существующие статьи в markdown
- Настроить поля для редактирования

### Шаг 4: Интеграция в Next.js

- Подключить Tina к страницам
- Настроить генерацию статических страниц

### Шаг 5: Настройка админки

- Настроить Tina Studio для редактирования
- Добавить права доступа (опционально)

---

## 🔄 Гибридный подход (рекомендуется)

**Что оставить в коде:**
- Структурные данные (regions.ts, products-prices.ts)
- Логика и компоненты
- Конфигурация

**Что вынести в CMS:**
- Статьи и текстовый контент
- Региональные SEO-тексты
- FAQ
- Мета-теги (опционально)

**Преимущества:**
- Контент-менеджеры могут редактировать тексты
- Разработчики работают с кодом как обычно
- Оптимизация сохраняется

---

## 🚀 Быстрый старт с Tina CMS

### 1. Установка

```bash
npm install tinacms @tinacms/cli
npx @tinacms/cli init
```

### 2. Создание схемы для статей

```typescript
// tina/config.ts
import { defineConfig } from 'tinacms'

export default defineConfig({
  clientId: process.env.NEXT_PUBLIC_TINA_CLIENT_ID,
  branch: process.env.NEXT_PUBLIC_TINA_BRANCH || 'main',
  token: process.env.TINA_TOKEN,
  build: {
    outputFolder: 'admin',
    publicFolder: 'public',
  },
  schema: {
    collections: [
      {
        name: 'article',
        label: 'Статьи',
        path: 'construction-articles',
        fields: [
          {
            type: 'string',
            name: 'title',
            label: 'Заголовок',
            required: true,
          },
          {
            type: 'rich-text',
            name: 'body',
            label: 'Содержание',
            isBody: true,
          },
        ],
      },
    ],
  },
})
```

### 3. Использование в Next.js

```typescript
// app/construction/[slug]/page.tsx
import { client } from '../../../tina/__generated__/client'

export default async function ArticlePage({ params }) {
  const { data } = await client.queries.article({
    relativePath: `${params.slug}.md`,
  })
  
  return (
    <article>
      <h1>{data.article.title}</h1>
      <TinaMarkdown content={data.article.body} />
    </article>
  )
}
```

---

## 📊 Сравнение вариантов

| Критерий | Tina CMS | Payload CMS | Sanity | Strapi |
|----------|----------|-------------|--------|--------|
| Данные в Git | ✅ Да | ❌ Нет | ❌ Нет | ❌ Нет |
| Работа через код | ✅ Да | ⚠️ Частично | ⚠️ Через API | ⚠️ Через API |
| Оптимизация SSG | ✅ Да | ✅ Да | ✅ Да (ISR) | ✅ Да (ISR) |
| Админка | ✅ Хорошая | ✅ Отличная | ✅ Отличная | ✅ Отличная |
| Сложность настройки | ⭐⭐ Средняя | ⭐⭐⭐ Высокая | ⭐⭐ Средняя | ⭐⭐⭐ Высокая |
| Стоимость | ✅ Бесплатно | ✅ Бесплатно | ✅ Бесплатно (план) | ✅ Бесплатно |
| Нужен сервер | ❌ Нет | ⚠️ Локально | ❌ Нет | ⚠️ Да |

---

## 🎯 Итоговая рекомендация

**Для вашего проекта рекомендую Tina CMS**, потому что:

1. ✅ Сохраняет работу через код
2. ✅ Данные в репозитории
3. ✅ Не влияет на оптимизацию
4. ✅ Удобная админка для контента
5. ✅ Бесплатно

**Альтернатива:** Если нужна более мощная админка и не критично хранить данные в Git, можно использовать **Payload CMS** с локальным SQLite хранилищем.

---

## 📚 Полезные ссылки

- [Tina CMS документация](https://tina.io/docs/)
- [Payload CMS документация](https://payloadcms.com/docs)
- [Sanity документация](https://www.sanity.io/docs)
- [Strapi документация](https://docs.strapi.io/)

---

**Вопросы?** Могу помочь с интеграцией любого из вариантов! 🚀

