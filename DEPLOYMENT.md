# Инструкция по обновлению сайта на сервере

## 🚀 Быстрое обновление

```bash
# На сервере (91.229.8.154)
cd /var/www/gazobeton-online
git pull origin main
npm install  # только если обновился package.json
npm run build
pm2 restart gazobeton-online
pm2 save
```

---

## 📋 Полная инструкция с сохранением файлов подтверждения

### Важно: Файлы подтверждения прав

На сервере есть файлы подтверждения прав для поисковых систем (например, `yandex_e18266fd8cfea0dd.html`). Эти файлы **НЕ в git**, но их нужно сохранить при обновлении.

### Автоматическое сохранение и восстановление

На сервере созданы скрипты для автоматического сохранения и восстановления:

```bash
# 1. Сохранить файлы подтверждения (перед обновлением)
/root/backup_verification_files.sh

# 2. Обновить код
cd /var/www/gazobeton-online
git pull origin main

# 3. Восстановить файлы подтверждения (после обновления)
/root/restore_verification_files.sh

# 4. Пересобрать и перезапустить
npm install  # только если обновился package.json
npm run build
pm2 restart gazobeton-online
pm2 save
```

---

## 🔄 Полный процесс обновления (рекомендуется)

```bash
# Подключиться к серверу
ssh root@91.229.8.154

# 1. Сохранить файлы подтверждения
/root/backup_verification_files.sh

# 2. Перейти в директорию проекта
cd /var/www/gazobeton-online

# 3. Обновить код
git pull origin main

# 4. Восстановить файлы подтверждения
/root/restore_verification_files.sh

# 5. Установить зависимости (если нужно)
npm install

# 6. Пересобрать проект
npm run build

# 7. Перезапустить приложение
pm2 restart gazobeton-online
pm2 save

# 8. Проверить статус
pm2 status
pm2 logs gazobeton-online --lines 20
```

---

## ⚠️ Что делать, если файлы подтверждения удалились

Если файлы подтверждения всё же удалились (например, после `git clean` или `git reset --hard`):

1. **Для Яндекс.Вебмастера:**
   - Зайдите в https://webmaster.yandex.ru/
   - Перейдите в настройки сайта
   - Выберите "Подтвердить права" → "HTML-файл"
   - Скачайте новый файл и загрузите на сервер:
     ```bash
     # Загрузить файл на сервер
     scp yandex_XXXXX.html root@91.229.8.154:/var/www/gazobeton-online/public/
     ```

2. **Проверить доступность:**
   ```bash
   curl https://gazobeton-online.ru/yandex_XXXXX.html
   ```

---

## 📝 Файлы, которые НЕ должны попадать в git

Следующие файлы добавлены в `.gitignore` и не будут удаляться при обновлении:

- `public/yandex_*.html` — файлы подтверждения Яндекс.Вебмастера
- `public/google*.html` — файлы подтверждения Google Search Console
- `public/bing*.html` — файлы подтверждения Bing Webmaster Tools
- `.env.local` — переменные окружения
- `deploy_env.txt` — временные файлы деплоя

---

## 🔍 Проверка после обновления

```bash
# Проверить версию на сервере
cd /var/www/gazobeton-online
git log -1 --oneline

# Проверить статус PM2
pm2 status

# Проверить логи
pm2 logs gazobeton-online --lines 50

# Проверить доступность сайта
curl -I https://gazobeton-online.ru

# Проверить файлы подтверждения
ls -la /var/www/gazobeton-online/public/yandex_*.html
curl https://gazobeton-online.ru/yandex_e18266fd8cfea0dd.html
```

---

## 🛠️ Ручное обновление (если скрипты не работают)

```bash
# 1. Сохранить файлы вручную
mkdir -p /root/verification_files_backup
cp /var/www/gazobeton-online/public/yandex_*.html /root/verification_files_backup/ 2>/dev/null || true

# 2. Обновить код
cd /var/www/gazobeton-online
git pull origin main

# 3. Восстановить файлы
cp /root/verification_files_backup/yandex_*.html /var/www/gazobeton-online/public/ 2>/dev/null || true

# 4. Пересобрать и перезапустить
npm run build
pm2 restart gazobeton-online
```

---

**Дата создания:** 13 ноября 2025  
**Последнее обновление:** 13 ноября 2025
