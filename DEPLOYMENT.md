# Инструкция по развертыванию на продакшн-сервере

## Безопасное хранение переменных окружения

### Важно: `.env.local` НЕ должен попадать в Git

✅ Файл `.env.local` уже добавлен в `.gitignore`, поэтому он не будет закоммичен в репозиторий.

---

## Вариант 1: Создание `.env.local` на сервере (рекомендуется)

### Шаг 1: Подключитесь к серверу

```bash
ssh root@91.229.8.154
# или
ssh ваш_пользователь@91.229.8.154
```

### Шаг 2: Перейдите в директорию проекта

```bash
cd /var/www/gazobeton-online
```

### Шаг 3: Создайте файл `.env.local`

```bash
nano .env.local
```

Вставьте следующее содержимое (замените на ваши реальные данные):

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=7694594630:AAE0trm0m1RQE95MWTX72GwdrFAsqYb5fKQ
TELEGRAM_CHAT_ID=423456833
```

Сохраните файл: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

### Шаг 4: Установите правильные права доступа

```bash
chmod 600 .env.local
chown ваш_пользователь:ваш_пользователь .env.local
```

Это ограничит доступ к файлу только для владельца.

### Шаг 5: Перезапустите приложение

```bash
pm2 restart gazobeton-online
# или
pm2 restart all
```

---

## Вариант 2: Использование переменных окружения в pm2

### Шаг 1: Создайте файл конфигурации pm2

```bash
cd /var/www/gazobeton-online
nano ecosystem.config.js
```

Вставьте:

```javascript
module.exports = {
  apps: [{
    name: 'gazobeton-online',
    script: 'npm',
    args: 'run start',
    cwd: '/var/www/gazobeton-online',
    env: {
      NODE_ENV: 'production',
      TELEGRAM_BOT_TOKEN: '7694594630:AAE0trm0m1RQE95MWTX72GwdrFAsqYb5fKQ',
      TELEGRAM_CHAT_ID: '423456833',
    }
  }]
};
```

**⚠️ Внимание:** Этот файл содержит секретные данные. Добавьте его в `.gitignore`:

```bash
echo "ecosystem.config.js" >> .gitignore
```

### Шаг 2: Перезапустите pm2 с новой конфигурацией

```bash
pm2 delete gazobeton-online
pm2 start ecosystem.config.js
pm2 save
```

---

## Вариант 3: Использование системных переменных окружения

### Шаг 1: Добавьте переменные в системный профиль

```bash
sudo nano /etc/environment
```

Добавьте в конец файла:

```
TELEGRAM_BOT_TOKEN=7694594630:AAE0trm0m1RQE95MWTX72GwdrFAsqYb5fKQ
TELEGRAM_CHAT_ID=423456833
```

### Шаг 2: Перезагрузите переменные окружения

```bash
source /etc/environment
pm2 restart gazobeton-online
```

---

## Проверка работы

### Тест 1: Проверьте, что переменные загружены

```bash
cd /var/www/gazobeton-online
node -e "console.log('Token:', process.env.TELEGRAM_BOT_TOKEN ? '✅ Загружен' : '❌ Не найден')"
```

### Тест 2: Отправьте тестовую заявку

Откройте сайт в браузере и отправьте тестовую заявку через любую форму. Проверьте, что сообщение пришло в Telegram.

### Тест 3: Проверьте логи

```bash
pm2 logs gazobeton-online --lines 50
```

Ищите ошибки типа "Telegram bot not configured" — их быть не должно.

---

## Обновление проекта на сервере

### Стандартный процесс обновления:

```bash
# 1. Подключитесь к серверу
ssh root@91.229.8.154

# 2. Перейдите в директорию проекта
cd /var/www/gazobeton-online

# 3. Получите последние изменения из Git
git pull origin main

# 4. Установите зависимости (если нужно)
npm install

# 5. Соберите проект
npm run build

# 6. Перезапустите приложение
pm2 restart gazobeton-online

# 7. Сохраните конфигурацию pm2
pm2 save
```

**Важно:** При обновлении `.env.local` на сервере НЕ перезаписывается, так как он в `.gitignore`.

---

## Безопасность

### ✅ Что уже сделано:

1. `.env.local` добавлен в `.gitignore` — файл не попадает в Git
2. Переменные окружения читаются только на сервере
3. API route проверяет наличие переменных перед использованием

### 🔒 Дополнительные меры безопасности:

1. **Ограничьте доступ к файлу:**
   ```bash
   chmod 600 .env.local
   ```

2. **Не храните секреты в коде:**
   - ✅ Никогда не коммитьте `.env.local`
   - ✅ Не добавляйте токены в комментарии
   - ✅ Не отправляйте токены в открытом виде

3. **Регулярно обновляйте токены:**
   - Если токен скомпрометирован, создайте новый через @BotFather
   - Обновите `.env.local` на сервере
   - Перезапустите приложение

---

## Резервное копирование

### Сохраните копию `.env.local` в безопасном месте:

```bash
# На сервере
cat .env.local
# Скопируйте содержимое и сохраните в безопасном месте (например, в менеджере паролей)
```

**⚠️ НЕ храните копию в Git или публичных местах!**

---

## Устранение проблем

### Проблема: "Telegram bot not configured"

**Решение:**
1. Проверьте, что `.env.local` существует на сервере:
   ```bash
   ls -la /var/www/gazobeton-online/.env.local
   ```

2. Проверьте содержимое файла:
   ```bash
   cat /var/www/gazobeton-online/.env.local
   ```

3. Убедитесь, что переменные названы правильно (без пробелов, без кавычек)

4. Перезапустите pm2:
   ```bash
   pm2 restart gazobeton-online
   ```

### Проблема: Заявки не приходят в Telegram

**Решение:**
1. Проверьте логи:
   ```bash
   pm2 logs gazobeton-online --lines 100
   ```

2. Проверьте токен бота:
   ```bash
   curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getMe"
   ```

3. Проверьте chat_id:
   - Отправьте сообщение боту
   - Проверьте через: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`

---

## Быстрая команда для первого деплоя

```bash
# На сервере выполните:
cd /var/www/gazobeton-online && \
cat > .env.local << 'EOF'
TELEGRAM_BOT_TOKEN=7694594630:AAE0trm0m1RQE95MWTX72GwdrFAsqYb5fKQ
TELEGRAM_CHAT_ID=423456833
EOF
chmod 600 .env.local && \
pm2 restart gazobeton-online
```

---

**Готово!** Теперь ваши секретные данные защищены и не попадут в Git.

