# Быстрый деплой на сервер

## ✅ Безопасность: ваши данные защищены

- `.env.local` уже в `.gitignore` — файл **НЕ попадет в Git**
- Токены будут храниться только на сервере

---

## 🚀 Быстрая настройка (5 минут)

### 1. Подключитесь к серверу

```bash
ssh root@91.229.8.154
# или ваш пользователь
```

### 2. Перейдите в директорию проекта

```bash
cd /var/www/gazobeton-online
```

### 3. Создайте файл с секретами

```bash
nano .env.local
```

Вставьте (скопируйте и вставьте):

```env
TELEGRAM_BOT_TOKEN=7694594630:AAE0trm0m1RQE95MWTX72GwdrFAsqYb5fKQ
TELEGRAM_CHAT_ID=423456833
```

Сохраните: `Ctrl+O` → `Enter` → `Ctrl+X`

### 4. Защитите файл

```bash
chmod 600 .env.local
```

Это ограничит доступ только для вас.

### 5. Перезапустите приложение

```bash
pm2 restart gazobeton-online
```

### 6. Проверьте логи

```bash
pm2 logs gazobeton-online --lines 20
```

Не должно быть ошибок "Telegram bot not configured".

---

## ✅ Готово!

Теперь все заявки с сайта будут приходить в ваш Telegram.

---

## 📝 Обновление проекта в будущем

Когда будете обновлять код на сервере:

```bash
cd /var/www/gazobeton-online
git pull origin main
npm install  # если нужно
npm run build
pm2 restart gazobeton-online
```

**Важно:** `.env.local` на сервере **НЕ перезапишется** при `git pull`, так как он в `.gitignore`.

---

## 🔒 Безопасность

- ✅ `.env.local` не попадет в Git (уже в `.gitignore`)
- ✅ Файл защищен правами доступа (`chmod 600`)
- ✅ Токены хранятся только на сервере
- ✅ При обновлении кода файл не перезапишется

---

## 🧪 Проверка работы

1. Откройте сайт в браузере
2. Заполните любую форму (квиз, калькулятор, контакты)
3. Отправьте заявку
4. Проверьте Telegram — должно прийти сообщение

---

## ❓ Проблемы?

Если заявки не приходят:

```bash
# Проверьте, что файл существует
ls -la .env.local

# Проверьте содержимое (будьте осторожны, не показывайте токен публично)
cat .env.local

# Проверьте логи
pm2 logs gazobeton-online --lines 50
```

Подробная инструкция: см. `DEPLOYMENT.md`

