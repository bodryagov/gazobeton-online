# 🚀 Чек-лист деплоя (для быстрого доступа)

## Просто скажи: "залей на сервер" или "сделай деплой"

Я автоматически выполню все шаги:

1. ✅ Проверю, что проект собирается (`npm run build`)
2. ✅ Создам тег для точки восстановления (`stable-YYYY-MM-DD`)
3. ✅ Закоммичу изменения с понятным описанием
4. ✅ Отправлю в GitHub (`git push`)
5. ✅ Обновлю на сервере (`git pull`, `npm run build`, `pm2 restart`)

---

## Если нужно что-то специфичное:

### Вариант 1: Обычный деплой (по умолчанию)
Просто скажи: **"залей на сервер"** или **"сделай деплой"**

### Вариант 2: Без создания тега
Скажи: **"залей на сервер, но без тега"**

### Вариант 3: Только локальный коммит (без деплоя)
Скажи: **"закоммить изменения, но не деплоить"**

### Вариант 4: Откат на сервере
Скажи: **"откати на сервере к тегу stable-2025-11-12"** (укажи нужный тег)

---

## Что я буду делать автоматически:

```
1. git status                    # Проверю изменения
2. npm run build                # Проверю, что собирается
3. git tag stable-YYYY-MM-DD    # Создам точку восстановления
4. git add .                    # Добавлю изменения
5. git commit -m "..."          # Закоммичу с описанием
6. git push origin main --tags  # Отправлю в GitHub
7. На сервере:
   - git pull origin main
   - npm run build
   - pm2 restart gazobeton-online
```

---

## Важно помнить:

- ✅ Я всегда создаю тег перед деплоем (точка восстановления)
- ✅ Я всегда проверяю `npm run build` перед отправкой
- ✅ Я всегда перезапускаю pm2 на сервере
- ✅ Все изменения логируются в коммитах

---

## Если что-то пошло не так:

Скажи: **"откати на сервере к последнему стабильному тегу"** — я найду последний тег и откачу.

