# Ежедневный workflow: разработка и деплой

## 📋 Порядок действий для ежедневной работы

### День 1: Разработка и тестирование локально

#### 1. Утром: проверить статус проекта
```bash
git status  # Посмотреть, есть ли незакоммиченные изменения
git pull origin main  # Получить последние изменения (если работаете с нескольких мест)
```

#### 2. В течение дня: разработка
- Вносите изменения в код
- Тестируете локально: `npm run dev`
- Проверяете на `http://localhost:3000`

#### 3. Перед заливкой на сервер: финальная проверка
```bash
# Проверить, что все работает
npm run build  # Убедиться, что проект собирается без ошибок
npm run lint   # Проверить код на ошибки

# Посмотреть, что изменилось
git status
git diff  # Просмотреть все изменения
```

#### 4. Создать тег (точку восстановления) ПЕРЕД заливкой на продакшн
```bash
# Создать тег с датой и описанием
git tag -a v1.0.0-2025-11-12 -m "Стабильная версия перед заливкой изменений"

# Или более простой вариант (без описания)
git tag v1.0.0-2025-11-12

# Посмотреть все теги
git tag

# Отправить теги на GitHub
git push origin --tags
```

#### 5. Закоммитить и отправить в GitHub
```bash
# Добавить все изменения
git add .

# Создать коммит с понятным описанием
git commit -m "Описание изменений: что сделано, что исправлено"

# Отправить в GitHub
git push origin main
```

#### 6. Обновить на сервере
```bash
# Подключиться к серверу
ssh root@91.229.8.154

# Перейти в директорию проекта
cd /var/www/gazobeton-online

# Получить изменения
git pull origin main

# Получить теги (если создавали)
git fetch --tags

# Пересобрать проект
npm run build

# Перезапустить приложение
pm2 restart gazobeton-online

# Проверить статус
pm2 status

# Проверить логи (если нужно)
pm2 logs gazobeton-online --lines 50

# Выйти
exit
```

#### 7. Проверить сайт на сервере
- Откройте `http://91.229.8.154` (или ваш домен)
- Убедитесь, что все работает корректно

---

### День 2: Если что-то сломалось на сервере

#### Вариант А: Быстрый откат к вчерашней версии (через тег)

```bash
# 1. Подключиться к серверу
ssh root@91.229.8.154
cd /var/www/gazobeton-online

# 2. Посмотреть доступные теги (точки восстановления)
git tag

# 3. Откатиться к нужному тегу (например, вчерашнему)
git checkout v1.0.0-2025-11-12

# 4. Пересобрать и перезапустить
npm run build
pm2 restart gazobeton-online

# 5. Проверить, что все работает
pm2 status
curl -I http://localhost:3000

# 6. Выйти
exit
```

#### Вариант Б: Откат к конкретному коммиту

```bash
# 1. Подключиться к серверу
ssh root@91.229.8.154
cd /var/www/gazobeton-online

# 2. Посмотреть историю коммитов
git log --oneline -10

# 3. Откатиться к нужному коммиту (скопировать хеш коммита)
git checkout 0fd0e6a

# 4. Пересобрать и перезапустить
npm run build
pm2 restart gazobeton-online

# 5. Проверить
pm2 status
exit
```

#### Вариант В: Откат только одного файла

```bash
# 1. Подключиться к серверу
ssh root@91.229.8.154
cd /var/www/gazobeton-online

# 2. Восстановить конкретный файл из предыдущего коммита
git checkout HEAD~1 -- app/page.tsx

# 3. Пересобрать и перезапустить
npm run build
pm2 restart gazobeton-online

# 4. Выйти
exit
```

---

### День 2: После отката — исправление проблемы локально

#### 1. Локально: найти и исправить проблему
```bash
# Посмотреть, что было в проблемном коммите
git show HEAD  # Последний коммит
git diff HEAD~1 HEAD  # Что изменилось

# Исправить проблему в коде
# Протестировать локально
npm run dev
npm run build
```

#### 2. Закоммитить исправление
```bash
git add .
git commit -m "Исправление: описание проблемы и решения"
git push origin main
```

#### 3. Обновить на сервере (после исправления)
```bash
ssh root@91.229.8.154
cd /var/www/gazobeton-online

# Вернуться на основную ветку (если откатывались через checkout)
git checkout main

# Получить исправления
git pull origin main

# Пересобрать и перезапустить
npm run build
pm2 restart gazobeton-online

# Проверить
pm2 status
exit
```

---

## 🏷️ Система тегов (рекомендуется)

### Создание тегов перед каждым деплоем

**Формат тега**: `v1.0.0-YYYY-MM-DD` или `stable-YYYY-MM-DD`

```bash
# Создать тег
git tag -a stable-2025-11-12 -m "Стабильная версия перед деплоем"

# Отправить теги на GitHub
git push origin --tags
```

### Использование тегов для отката

```bash
# На сервере: посмотреть все теги
git tag

# Откатиться к тегу
git checkout stable-2025-11-12
npm run build
pm2 restart gazobeton-online
```

---

## 📝 Чек-лист перед каждым деплоем

- [ ] Локально все работает (`npm run dev`)
- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Нет ошибок линтера (`npm run lint`)
- [ ] Создан тег для точки восстановления (`git tag stable-YYYY-MM-DD`)
- [ ] Тег отправлен на GitHub (`git push origin --tags`)
- [ ] Изменения закоммичены (`git commit`)
- [ ] Изменения отправлены в GitHub (`git push`)
- [ ] На сервере получены изменения (`git pull`)
- [ ] Проект пересобран (`npm run build`)
- [ ] Приложение перезапущено (`pm2 restart`)
- [ ] Сайт проверен в браузере

---

## ⚠️ Важные замечания

1. **Всегда создавайте тег перед деплоем** — это ваша точка восстановления
2. **Тестируйте локально** перед отправкой на сервер
3. **Проверяйте `npm run build`** — если не собирается локально, не соберется и на сервере
4. **После отката через `git checkout`** не забудьте вернуться на `main` перед следующим деплоем
5. **Логи на сервере**: `pm2 logs gazobeton-online` — помогут найти проблему

---

## 🔄 Быстрая справка

### Ежедневный деплой (когда все работает)
```bash
# Локально
git add .
git commit -m "Описание"
git tag stable-$(date +%Y-%m-%d)
git push origin main --tags

# На сервере
ssh root@91.229.8.154
cd /var/www/gazobeton-online
git pull origin main
npm run build
pm2 restart gazobeton-online
exit
```

### Откат на сервере (если что-то сломалось)
```bash
ssh root@91.229.8.154
cd /var/www/gazobeton-online
git tag  # Посмотреть теги
git checkout stable-2025-11-12  # Откатиться к тегу
npm run build
pm2 restart gazobeton-online
exit
```

